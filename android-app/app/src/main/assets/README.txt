Assets used by the demo:
- touch_ae.tflite : TFLite autoencoder model (expects [1,N] input)
- scaler.json     : StandardScaler parameters {"mean": [...], "scale": [...]} 
- threshold.json  : {"threshold": <float>}

